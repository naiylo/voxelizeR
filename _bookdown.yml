book_filename: "voxelizer_vignette"
rmd_files: ["vignettes/voxelizer_vignette.Rmd"]
output_dir: "docs"
new_session: yes

bookdown::gitbook:
  bookdown::gitbook:
    split_by: chapter+number  # Splits by chapter
  config:
    toc:
      collapse: section
      before: |
        <li><a href="./">Home</a></li>
    edit: true
    number_sections: true # Enable numbering for all sections
    section_numbering:
      level_1: true   # Enable numbering for top-level headers
      level_2: false   # Enable numbering for second-level headers
      level_3: false  # Disable numbering for third-level headers
      level_4: false  # Disable numbering for fourth-level headers

